name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run: echo Hello, world!
    - name: Run a multi-line script
      run: |
        npm i
        npm run build
        mv public/* docs/
        git config --global user.email "vladkhvo@gmail.com"
        git config --global user.name "vladislav khvsotov"
        git checkout master
        git add ./docs
        git status
        git commit -m 'deploy static in docs'
